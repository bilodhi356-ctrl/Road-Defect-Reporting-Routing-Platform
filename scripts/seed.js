require('dotenv').config();const bcrypt=require('bcryptjs');const {connectDb}=require('../src/db');const User=require('../src/models/User');
(async()=>{await connectDb();const email=process.env.STAFF_EMAIL||'admin@kerra.go.ke';const name=process.env.STAFF_NAME||'Jane Wanjiru';const hash=await bcrypt.hash(process.env.STAFF_PASSWORD||'Password112',12);await User.findOneAndUpdate({email},{name,email,passwordHash:hash,role:'admin',office:'Central Regional Office',active:true},{upsert:true,new:true,setDefaultsOnInsert:true});console.log(`Staff account ready: ${email}`);process.exit(0)})().catch(e=>{console.error(e);process.exit(1)});
