const fs=require('fs');const path=require('path');function log(error,req){const dir=path.join(process.cwd(),'logs');fs.mkdirSync(dir,{recursive:true});fs.appendFileSync(path.join(dir,'error.log'),`${new Date().toISOString()} ${req.method} ${req.originalUrl} ${error.stack||error}\n`);}function notFound(req,res){res.status(404).json({message:'Route not found.'});}function errorHandler(error,req,res,_next){log(error,req);if(error.code===11000)return res.status(409).json({message:'That record already exists.'});if(error.name==='MulterError')return res.status(400).json({message:error.message});res.status(error.status||500).json({message:error.status?error.message:'An unexpected server error occurred.'});}module.exports={errorHandler,notFound};
